# 采购总额与库存价值不一致问题修复说明

## 问题描述
系统中显示的采购总额（¥25,518.00）与库存价值（¥25,845.00）不一致。

## 问题原因
经过分析，发现以下几个问题导致了数据不一致：

1. **Excel转换脚本问题**：`convert_excel_to_json.py` 为每行Excel数据创建了独立的服装和库存记录，导致大量重复记录。

2. **导入逻辑问题**：`Settings.jsx` 在导入数据时，处理重复记录的逻辑存在缺陷，导致部分数据被错误累加或跳过。

3. **字段缺失问题**：入库记录缺少 `code`、`name`、`category` 等关键字段，导致部分记录在导入时无法正确关联对应的服装信息。

## 修复方案

### 1. 修复Excel转换脚本
在 `convert_excel_to_json.py` 中：
- 添加了去重逻辑，使用 `code+color+size` 作为服装的唯一标识
- 为相同的服装记录累加库存数量
- 为入库记录添加了完整的字段（code、name、category、sellingPrice等）

### 2. 修复导入逻辑
在 `Settings.jsx` 中：
- 改进了库存记录与服装记录的关联逻辑
- 确保每个库存记录都能正确找到对应的服装ID
- 避免了重复记录的错误累加

## 修复后的效果

### 转换结果统计
- 原始数据行数：229行
- 重复商品数量：2个
- 服装记录数：227条（去重后）
- 库存记录数：227条（与服装记录一一对应）
- 入库记录数：229条（保留所有原始入库记录）

### 数据一致性检查
- 入库总数量：362件
- 采购总额：25,845.00元
- 库存总数量：362件
- 库存总价值：25,845.00元
- ✅ 采购总额和库存价值完全一致！

## 如何更新系统数据

1. **重新导入修复后的JSON文件**：
   - 打开系统设置页面
   - 点击"导入数据"
   - 选择 `转换后的进货单.json` 文件
   - 完成导入

2. **验证修复结果**：
   - 导入完成后，刷新页面
   - 查看库存概览页面的采购总额和库存价值
   - 两者应该都显示为 ¥25,845.00 且保持一致

## 注意事项

1. 本次修复会替换系统中的所有数据，请确保在导入前已经备份了重要数据。

2. 导入过程可能需要几秒钟，请耐心等待，不要中途关闭页面。

3. 导入完成后，建议检查几个关键页面（库存概览、报表统计）确保数据显示正常。

## 文件更新列表

1. `convert_excel_to_json.py` - 修复Excel转换逻辑
2. `Settings.jsx` - 修复数据导入逻辑
3. `转换后的进货单.json` - 修复后的JSON数据文件

---

修复完成后，系统中的采购总额和库存价值将保持一致，确保数据的准确性和可靠性。
